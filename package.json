{
  "name": "winshot-browser-capture",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local-first WinShot browser capture extension and test harness",
  "scripts": {
    "build:extension": "node browser-extension/tools/build-extension.mjs",
    "test:browser": "node --test browser-extension/tests/unit/*.test.mjs",
    "test:e2e": "npm run build:extension && playwright test -c browser-extension/playwright.config.mjs",
    "test:extension": "npm run test:browser && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "puppeteer": "25.4.0"
  }
}
