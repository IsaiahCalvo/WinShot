<Window x:Class="WinShot.Overlay.QuickActionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" SizeToContent="WidthAndHeight" WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent" Topmost="True"
        ShowInTaskbar="False" ShowActivated="False" WindowStartupLocation="Manual">
    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4F4F4F"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Border CornerRadius="10" Background="#F22B2B2B" BorderBrush="#33FFFFFF" BorderThickness="1"
            Padding="10" MouseLeftButtonDown="OnDragMove">
        <StackPanel>
            <Border CornerRadius="6" ClipToBounds="True" HorizontalAlignment="Center">
                <Image x:Name="Thumb" Stretch="Uniform" MaxWidth="320" MaxHeight="180"/>
            </Border>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button x:Name="BtnCopy" Style="{StaticResource ActionButton}" Content="Copy" Click="OnCopy"/>
                <Button x:Name="BtnSave" Style="{StaticResource ActionButton}" Content="Save" Click="OnSave"/>
                <Button x:Name="BtnEdit" Style="{StaticResource ActionButton}" Content="Edit" Click="OnEdit"/>
                <Button x:Name="BtnPin" Style="{StaticResource ActionButton}" Content="Pin" Click="OnPin"/>
                <Button x:Name="BtnOcr" Style="{StaticResource ActionButton}" Content="OCR" Click="OnOcr"/>
                <Button x:Name="BtnClose" Style="{StaticResource ActionButton}" Content="✕" Click="OnClose"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
