<Window x:Class="WinShot.Recording.VideoEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinShot — Video Editor" Width="760" Height="600" MinWidth="640" MinHeight="480"
        Background="#2B2B2B" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="#3A3A3A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="12,5"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4F4F4F"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Padding" Value="18,6"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FieldLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="#DDDDDD"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="DarkCombo" TargetType="ComboBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Focusable="False" ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="Bd" Background="#3A3A3A" BorderBrush="#555555"
                                                BorderThickness="1" CornerRadius="4">
                                            <Path HorizontalAlignment="Right" VerticalAlignment="Center"
                                                  Margin="0,0,8,0" Data="M 0 0 L 4 4 L 8 0 Z" Fill="#BBBBBB"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="#454545"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Margin="8,0,24,0" VerticalAlignment="Center" IsHitTestVisible="False"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>
                            <Popup x:Name="PART_Popup" Placement="Bottom" AllowsTransparency="True"
                                   Focusable="False" PopupAnimation="Slide"
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}">
                                <Border Background="#2F2F2F" BorderBrush="#555555" BorderThickness="1"
                                        CornerRadius="4" Margin="0,2,0,0" MaxHeight="220"
                                        MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                                    <ScrollViewer>
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="Bd" Background="Transparent" Padding="{TemplateBinding Padding}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsHighlighted" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SliderRepeat" TargetType="RepeatButton">
            <Setter Property="IsTabStop" Value="False"/>
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SliderFill" TargetType="RepeatButton" BasedOn="{StaticResource SliderRepeat}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RepeatButton">
                        <Border Background="{StaticResource AccentBrush}" Height="4" CornerRadius="2" VerticalAlignment="Center"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkSlider" TargetType="Slider">
            <Setter Property="Focusable" Value="False"/>
            <Setter Property="IsMoveToPointEnabled" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid Height="20" Background="Transparent">
                            <Border Height="4" CornerRadius="2" Background="#3A3A3A" VerticalAlignment="Center"/>
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderFill}" Command="Slider.DecreaseLarge"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Style="{StaticResource SliderRepeat}" Command="Slider.IncreaseLarge"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="14" Height="14" Focusable="False">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Ellipse Fill="White" Stroke="{StaticResource AccentBrush}" StrokeThickness="1"/>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E1E" CornerRadius="8" ClipToBounds="True">
            <MediaElement x:Name="Media" LoadedBehavior="Manual" UnloadedBehavior="Manual"
                          ScrubbingEnabled="True" Stretch="Uniform"
                          MediaOpened="OnMediaOpened" MediaEnded="OnMediaEnded" MediaFailed="OnMediaFailed"/>
        </Border>

        <DockPanel Grid.Row="1" Margin="0,12,0,0">
            <Button x:Name="BtnPlay" DockPanel.Dock="Left" Style="{StaticResource ActionButton}"
                    Width="64" Content="Play" Click="OnPlayPause"/>
            <TextBlock x:Name="TimeLabel" DockPanel.Dock="Right" Foreground="#DDDDDD" FontSize="12"
                       FontFamily="Consolas" VerticalAlignment="Center" Margin="10,0,0,0" Text="0:00 / 0:00"/>
            <Slider x:Name="PositionSlider" Style="{StaticResource DarkSlider}" Margin="10,0,0,0"
                    Minimum="0" Maximum="1" ValueChanged="OnPositionSliderChanged"/>
        </DockPanel>

        <!-- Filmstrip trim timeline: a dark frame-thumbnail track with two
             draggable in/out handles and a live playhead. The two old trim
             sliders are gone; TrimStartSeconds/TrimEndSeconds now drive export. -->
        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border x:Name="TimelineTrack" Grid.Row="0" Height="56" CornerRadius="6"
                    Background="#1A1A1A" BorderBrush="#3A3A3A" BorderThickness="1"
                    ClipToBounds="True" SizeChanged="OnTimelineSizeChanged"
                    MouseLeftButtonDown="OnTimelineClicked">
                <Canvas x:Name="TimelineCanvas" ClipToBounds="True">
                    <!-- Frame thumbnails (best-effort) live here, added in code. -->
                    <ItemsControl x:Name="FilmstripItems" Canvas.Left="0" Canvas.Top="0">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>

                    <!-- Dimmers over the trimmed-away head and tail regions. -->
                    <Rectangle x:Name="LeftDim" Canvas.Left="0" Canvas.Top="0" Height="54"
                               Fill="#B0000000" Width="0" IsHitTestVisible="False"/>
                    <Rectangle x:Name="RightDim" Canvas.Top="0" Height="54"
                               Fill="#B0000000" Width="0" IsHitTestVisible="False"/>

                    <!-- Accent border around the kept region. -->
                    <Border x:Name="SelectionBorder" Canvas.Top="0" Height="54" Width="0"
                            BorderBrush="{StaticResource AccentBrush}" BorderThickness="2"
                            CornerRadius="4" IsHitTestVisible="False"/>

                    <!-- Playhead. -->
                    <Rectangle x:Name="Playhead" Canvas.Left="0" Canvas.Top="2" Width="2" Height="50"
                               Fill="White" IsHitTestVisible="False"/>

                    <!-- In / out drag handles. -->
                    <Thumb x:Name="TrimStartThumb" Canvas.Left="0" Canvas.Top="0" Width="12" Height="54"
                           Cursor="SizeWE" DragDelta="OnTrimStartDrag">
                        <Thumb.Template>
                            <ControlTemplate TargetType="Thumb">
                                <Grid Background="Transparent">
                                    <Border HorizontalAlignment="Right" Width="6" CornerRadius="3,0,0,3"
                                            Background="{StaticResource AccentBrush}"/>
                                    <Rectangle HorizontalAlignment="Right" Margin="0,0,2,0" Width="2" Height="18"
                                               RadiusX="1" RadiusY="1" Fill="White"/>
                                </Grid>
                            </ControlTemplate>
                        </Thumb.Template>
                    </Thumb>
                    <Thumb x:Name="TrimEndThumb" Canvas.Left="0" Canvas.Top="0" Width="12" Height="54"
                           Cursor="SizeWE" DragDelta="OnTrimEndDrag">
                        <Thumb.Template>
                            <ControlTemplate TargetType="Thumb">
                                <Grid Background="Transparent">
                                    <Border HorizontalAlignment="Left" Width="6" CornerRadius="0,3,3,0"
                                            Background="{StaticResource AccentBrush}"/>
                                    <Rectangle HorizontalAlignment="Left" Margin="2,0,0,0" Width="2" Height="18"
                                               RadiusX="1" RadiusY="1" Fill="White"/>
                                </Grid>
                            </ControlTemplate>
                        </Thumb.Template>
                    </Thumb>
                </Canvas>
            </Border>

            <Grid Grid.Row="1" Margin="0,6,0,0">
                <TextBlock x:Name="TrimStartLabel" HorizontalAlignment="Left" Style="{StaticResource FieldLabel}"
                           FontFamily="Consolas" Foreground="#AAAAAA" Text="In 0:00"/>
                <TextBlock x:Name="TrimRangeLabel" HorizontalAlignment="Center" Style="{StaticResource FieldLabel}"
                           FontFamily="Consolas" Foreground="#888888" Text="0:00"/>
                <TextBlock x:Name="TrimEndLabel" HorizontalAlignment="Right" Style="{StaticResource FieldLabel}"
                           FontFamily="Consolas" Foreground="#AAAAAA" Text="Out 0:00"/>
            </Grid>
        </Grid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,14,0,0">
            <TextBlock Style="{StaticResource FieldLabel}" Text="Resolution"/>
            <ComboBox x:Name="ResolutionCombo" Width="86" Margin="8,0,0,0" Style="{StaticResource DarkCombo}" SelectedIndex="0">
                <ComboBoxItem Content="100%"/>
                <ComboBoxItem Content="75%"/>
                <ComboBoxItem Content="50%"/>
            </ComboBox>
            <TextBlock Style="{StaticResource FieldLabel}" Text="Quality" Margin="18,0,0,0"/>
            <ComboBox x:Name="QualityCombo" Width="96" Margin="8,0,0,0" Style="{StaticResource DarkCombo}" SelectedIndex="0">
                <ComboBoxItem Content="High"/>
                <ComboBoxItem Content="Medium"/>
                <ComboBoxItem Content="Low"/>
            </ComboBox>
            <TextBlock Style="{StaticResource FieldLabel}" Text="FPS" Margin="18,0,0,0"/>
            <ComboBox x:Name="FrameRateCombo" Width="86" Margin="8,0,0,0" Style="{StaticResource DarkCombo}" SelectedIndex="0">
                <ComboBoxItem Content="Source"/>
                <ComboBoxItem Content="60"/>
                <ComboBoxItem Content="30"/>
                <ComboBoxItem Content="15"/>
            </ComboBox>
            <CheckBox x:Name="MuteCheck" Content="Mute" Margin="18,0,0,0"
                      Checked="OnMuteChanged" Unchecked="OnMuteChanged"/>
            <CheckBox x:Name="MonoCheck" Content="Mono" Margin="12,0,0,0"/>
            <TextBlock Style="{StaticResource FieldLabel}" Text="Volume" Margin="18,0,8,0"/>
            <Slider x:Name="VolumeSlider" Width="110" Style="{StaticResource DarkSlider}" VerticalAlignment="Center"
                    Minimum="0" Maximum="1" Value="1" ValueChanged="OnVolumeChanged"/>
        </StackPanel>

        <Grid Grid.Row="4" Margin="0,14,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel VerticalAlignment="Center" Margin="0,0,12,0">
                <TextBlock x:Name="StatusText" Foreground="#888888" FontSize="11" TextTrimming="CharacterEllipsis"/>
                <ProgressBar x:Name="ExportProgress" Height="6" Margin="0,4,0,0" Maximum="100"
                             Background="#3A3A3A" Foreground="{StaticResource AccentBrush}" BorderThickness="0"
                             Visibility="Collapsed"/>
            </StackPanel>
            <Button Grid.Column="1" x:Name="BtnExport" Style="{StaticResource AccentButton}"
                    Content="Export" Click="OnExport"/>
        </Grid>
    </Grid>
</Window>
