<Window x:Class="WinShot.Scrolling.ScrollingModeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="" SizeToContent="WidthAndHeight" WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent" Topmost="True"
        ShowInTaskbar="False" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource ElevatedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="DarkRadio" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,2"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style x:Key="RadioHint" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Margin" Value="18,0,0,8"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
        </Style>
    </Window.Resources>
    <Border CornerRadius="{StaticResource RadiusCard}" Background="{StaticResource FloatingBgBrush}"
            BorderBrush="{StaticResource TopHighlightBrush}" BorderThickness="1"
            Padding="18" Effect="{StaticResource FloatingShadow}" MouseLeftButtonDown="OnDragMove">
        <StackPanel Width="250">
            <TextBlock Text="Scrolling capture" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14"
                       FontWeight="SemiBold" Margin="0,0,0,12"/>
            <RadioButton x:Name="AutoRadio" Style="{StaticResource DarkRadio}" GroupName="ScrollMode"
                         Content="Auto-scroll" IsChecked="True"/>
            <TextBlock Style="{StaticResource RadioHint}"
                       Text="WinShot scrolls the content and stops at the bottom."/>
            <RadioButton x:Name="ManualRadio" Style="{StaticResource DarkRadio}" GroupName="ScrollMode"
                         Content="I'll scroll myself"/>
            <TextBlock Style="{StaticResource RadioHint}"
                       Text="Scroll at your own pace; click Stop when done."/>
            <TextBlock Text="Direction" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,6,0,4"/>
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="VerticalRadio" Style="{StaticResource DarkRadio}" GroupName="ScrollDirection"
                             Content="Vertical" IsChecked="True" Margin="0,0,16,2"/>
                <RadioButton x:Name="HorizontalRadio" Style="{StaticResource DarkRadio}" GroupName="ScrollDirection"
                             Content="Horizontal"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                <Button Style="{StaticResource ActionButton}" Content="Cancel" Click="OnCancel"/>
                <Button Style="{StaticResource AccentButton}" Content="Start" Click="OnStart" IsDefault="True"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
