<Window x:Class="WinShot.History.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinShot History" Width="1200" Height="760" MinWidth="720" MinHeight="480"
        Background="{StaticResource WindowBgBrush}" ShowInTaskbar="True" WindowStartupLocation="Manual"
        AutomationProperties.Name="WinShot history"
        AutomationProperties.HelpText="Browse, sort, select, restore, copy, edit, pin, share, reveal, or delete local captures.">
    <Window.Resources>
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource RadiusButton}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceBrush}"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="Bd" Property="Background" Value="{StaticResource ElevatedBrush}"/></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/><Setter TargetName="Bd" Property="BorderThickness" Value="1"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.42"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentToolbarButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="16,9"/>
        </Style>

        <Style x:Key="FilterChip" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="{StaticResource RadiusButton}" Padding="16,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceBrush}"/></Trigger>
                            <Trigger Property="IsChecked" Value="True"><Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/><Setter Property="Foreground" Value="White"/></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/><Setter TargetName="Bd" Property="BorderThickness" Value="1"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="OverlayActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="{StaticResource RadiusButton}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="#3AFFFFFF"/></Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="#AAFFFFFF"/><Setter TargetName="Bd" Property="BorderThickness" Value="1"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter Property="Opacity" Value="0.3"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="SelectionCommandRow" Grid.Row="1" Background="{StaticResource ToolbarBgBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1" Padding="16,10">
            <Grid>
                <StackPanel x:Name="NormalToolbar" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button x:Name="SelectButton" Content="Select" Style="{StaticResource AccentToolbarButton}"
                            Click="OnToggleSelectionMode" AutomationProperties.Name="Select history items"/>
                </StackPanel>

                <StackPanel x:Name="SelectionToolbar" Orientation="Horizontal" HorizontalAlignment="Left" Visibility="Collapsed">
                    <Button x:Name="DoneButton" Style="{StaticResource AccentToolbarButton}" Click="OnToggleSelectionMode">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE73E;" FontFamily="{StaticResource IconFont}" Margin="0,0,8,0"/><TextBlock Text="Done"/></StackPanel>
                    </Button>
                    <TextBlock x:Name="SelectionCountText" Text="0 selected" Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="13" VerticalAlignment="Center" Margin="20,0,16,0"/>
                    <Border Width="1" Background="{StaticResource BorderBrush}" Margin="0,2,8,2"/>
                    <Button x:Name="AllSelectionButton" Style="{StaticResource ToolbarButton}" Click="OnShowAllFromSelection" AutomationProperties.Name="Select or clear all visible history items">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE8A5;" FontFamily="{StaticResource IconFont}" Margin="0,0,8,0"/><TextBlock Text="All"/></StackPanel>
                    </Button>
                    <Button x:Name="SelectAllButton" Style="{StaticResource ToolbarButton}" Click="OnSelectAll" AutomationProperties.Name="Select all visible history items">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE762;" FontFamily="{StaticResource IconFont}" Margin="0,0,8,0"/><TextBlock Text="Select all"/></StackPanel>
                    </Button>
                    <Button x:Name="CopyToButton" Style="{StaticResource ToolbarButton}" Click="OnCopySelected" IsEnabled="False" AutomationProperties.Name="Copy selected files">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE8B0;" FontFamily="{StaticResource IconFont}" Margin="0,0,8,0"/><TextBlock Text="Copy to"/></StackPanel>
                    </Button>
                    <Button x:Name="PinSelectedButton" Style="{StaticResource ToolbarButton}" Click="OnPinSelected" IsEnabled="False" AutomationProperties.Name="Pin selected screenshots">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE840;" FontFamily="{StaticResource IconFont}" Margin="0,0,8,0"/><TextBlock Text="Pin"/></StackPanel>
                    </Button>
                    <Button x:Name="ShareButton" Style="{StaticResource ToolbarButton}" Click="OnShareSelected" IsEnabled="False" AutomationProperties.Name="Share selected files">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE72D;" FontFamily="{StaticResource IconFont}" Margin="0,0,8,0"/><TextBlock Text="Share"/><TextBlock Text="&#xE70D;" FontFamily="{StaticResource IconFont}" FontSize="9" Margin="8,1,0,0"/></StackPanel>
                    </Button>
                    <Button x:Name="DeleteSelectedButton" Style="{StaticResource ToolbarButton}" Click="OnDeleteSelected" IsEnabled="False" AutomationProperties.Name="Delete selected history items">
                        <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE74D;" FontFamily="{StaticResource IconFont}" Margin="0,0,8,0"/><TextBlock Text="Delete"/></StackPanel>
                    </Button>
                </StackPanel>

                <StackPanel x:Name="SortControls" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <TextBlock Text="Sort by:" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <Button x:Name="SortFieldButton" Style="{StaticResource ToolbarButton}" Click="OnSortFieldMenu" Background="{StaticResource SurfaceBrush}" Padding="12,7">
                        <StackPanel Orientation="Horizontal"><TextBlock x:Name="SortFieldText" Text="Date"/><TextBlock Text="&#xE70D;" FontFamily="{StaticResource IconFont}" FontSize="9" Margin="12,2,0,0"/></StackPanel>
                    </Button>
                    <Button x:Name="SortDirectionButton" Style="{StaticResource ToolbarButton}" Click="OnToggleSortDirection" Margin="4,0,0,0"
                            AutomationProperties.Name="Sort descending"><TextBlock x:Name="SortDirectionGlyph" Text="&#xE74B;" FontFamily="{StaticResource IconFont}" FontSize="16"/></Button>
                </StackPanel>
            </Grid>
        </Border>

        <Border x:Name="CategoryRow" Grid.Row="0" Background="{StaticResource WindowBgBrush}" Padding="16,12,16,10">
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <RadioButton x:Name="ChipAll" Content="All" IsChecked="True" Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="ChipImages" Content="Screenshots" Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="ChipScrolling" Content="Scrolling" Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="ChipVideo" Content="Videos" Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                    <RadioButton x:Name="ChipGif" Content="GIFs" Style="{StaticResource FilterChip}" Checked="OnFilterChanged"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="3" Background="{StaticResource ToolbarBgBrush}"
                BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,10">
            <DockPanel>
                <Button x:Name="LoadMoreButton" DockPanel.Dock="Right" Content="Load more" Style="{StaticResource ToolbarButton}"
                        Click="OnLoadMore" Visibility="Collapsed" AutomationProperties.Name="Load more history items"
                        AutomationProperties.HelpText="Shows the next 200 local history items without loading the entire library at once."/>
                <Button DockPanel.Dock="Right" Style="{StaticResource ToolbarButton}" Click="OnRefresh" Margin="0,0,8,0" AutomationProperties.Name="Refresh history">
                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE72C;" FontFamily="{StaticResource IconFont}" Margin="0,0,7,0"/><TextBlock Text="Refresh"/></StackPanel>
                </Button>
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="CountText" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12" VerticalAlignment="Center" AutomationProperties.LiveSetting="Polite"/>
                    <Border Width="1" Height="18" Background="{StaticResource BorderBrush}" Margin="16,0"/>
                    <TextBlock x:Name="SelectionSummaryText" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Grid Grid.Row="2">
            <ListBox x:Name="ItemsList" Tag="False" SelectionMode="Single"
                     Background="Transparent" BorderThickness="0" Padding="14,2,14,14"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled" ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.CanContentScroll="False" KeyboardNavigation.TabNavigation="Continue"
                     AutomationProperties.Name="Capture history"
                     AutomationProperties.HelpText="Use arrow keys to select, Enter to open, Space to preview, and Tab to reach item actions.">
                <ListBox.ItemsPanel><ItemsPanelTemplate><WrapPanel Orientation="Horizontal"/></ItemsPanelTemplate></ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="True"/><Setter Property="Margin" Value="0"/><Setter Property="Padding" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}"/>
                        <Setter Property="AutomationProperties.HelpText" Value="{Binding AccessibleHelpText}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <!-- Ring radius = thumbnail frame radius (8) + 2px border inset so they stay concentric. -->
                                    <Border x:Name="SelectionRing" Margin="6" BorderBrush="Transparent" BorderThickness="2" CornerRadius="10" Background="Transparent">
                                        <ContentPresenter/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True"><Setter TargetName="SelectionRing" Property="BorderBrush" Value="{StaticResource AccentBrush}"/></Trigger>
                                        <Trigger Property="IsKeyboardFocusWithin" Value="True"><Setter TargetName="SelectionRing" Property="BorderBrush" Value="{StaticResource AccentBrush}"/></Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border x:Name="Card" Width="260" Background="Transparent" MouseLeftButtonDown="OnTileMouseDown" MouseMove="OnTileMouseMove" MouseEnter="OnTileMouseEnter">
                            <Border.ContextMenu>
                                <ContextMenu Style="{StaticResource DarkContextMenu}">
                                    <MenuItem Header="Restore to Quick Access" Click="OnRestore" Visibility="{Binding ImageOnlyVisibility}" Style="{StaticResource DarkMenuItem}"/>
                                    <MenuItem Header="Copy" Click="OnCopy" Visibility="{Binding ImageOnlyVisibility}" Style="{StaticResource DarkMenuItem}"/>
                                    <MenuItem Header="Edit" Click="OnEdit" Visibility="{Binding ImageOnlyVisibility}" Style="{StaticResource DarkMenuItem}"/>
                                    <MenuItem Header="Pin" Click="OnPin" Visibility="{Binding PinVisibility}" Style="{StaticResource DarkMenuItem}"/>
                                    <MenuItem Header="Open" Click="OnOpen" Style="{StaticResource DarkMenuItem}"/>
                                    <MenuItem Header="Reveal in Explorer" Click="OnReveal" Style="{StaticResource DarkMenuItem}"/>
                                    <Separator/><MenuItem Header="Delete" Click="OnDelete" Style="{StaticResource DarkMenuItem}"/>
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Grid>
                                <Grid.RowDefinitions><RowDefinition Height="164"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                <Border Grid.Row="0" CornerRadius="{StaticResource RadiusButton}" ClipToBounds="True" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
                                    <Grid>
                                        <Grid x:Name="ThumbnailSurface">
                                            <Image Source="{Binding Thumbnail}" Stretch="UniformToFill" Visibility="{Binding ThumbnailVisibility}"/>
                                            <Grid Visibility="{Binding MediaFallbackVisibility}" Background="{StaticResource SurfaceBrush}">
                                                <TextBlock Text="{Binding ExtensionLabel}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="24" FontWeight="SemiBold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Grid>
                                            <Border Background="{StaticResource SurfaceBrush}" Opacity="0.72" Visibility="{Binding MissingThumbnailVisibility}">
                                                <TextBlock Text="&#xE91B;" FontFamily="{StaticResource IconFont}" FontSize="34" Foreground="{StaticResource TextDisabledBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <Border HorizontalAlignment="Right" VerticalAlignment="Top" Margin="7" Padding="7,3" CornerRadius="4" Background="#D91D1D1D" Visibility="{Binding ScrollingVisibility}">
                                                <TextBlock Text="SCROLLING" Foreground="White" FontSize="10" FontWeight="SemiBold"/>
                                            </Border>
                                            <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="7" Width="32" Height="25" CornerRadius="4" Background="#D91D1D1D" Visibility="{Binding VideoBadgeVisibility}">
                                                <TextBlock Text="&#xE768;" FontFamily="{StaticResource IconFont}" Foreground="White" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <Border HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="7" Padding="7,4" CornerRadius="4" Background="#D91D1D1D" Visibility="{Binding GifBadgeVisibility}">
                                                <TextBlock Text="GIF" Foreground="White" FontSize="10" FontWeight="SemiBold"/>
                                            </Border>
                                        </Grid>

                                        <!-- Bottom-anchored gradient scrim holding the action row: hover adds
                                             controls without blurring or blacking out the preview itself. -->
                                        <Border x:Name="OverlayActions" VerticalAlignment="Bottom" Height="56" Visibility="Collapsed">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                    <GradientStop Color="#00000000" Offset="0"/>
                                                    <GradientStop Color="#B3000000" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,3">
                                                <Button Style="{StaticResource OverlayActionButton}" Click="OnRestore" Visibility="{Binding ImageOnlyVisibility}" AutomationProperties.Name="Restore to Quick Access">
                                                    <StackPanel><TextBlock Text="&#xE72C;" FontFamily="{StaticResource IconFont}" FontSize="16" HorizontalAlignment="Center"/><TextBlock Text="Restore" FontSize="10" Margin="0,3,0,0"/></StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OverlayActionButton}" Click="OnCopy" Visibility="{Binding ImageOnlyVisibility}" AutomationProperties.Name="Copy screenshot">
                                                    <StackPanel><TextBlock Text="&#xE8C8;" FontFamily="{StaticResource IconFont}" FontSize="16" HorizontalAlignment="Center"/><TextBlock Text="Copy" FontSize="10" Margin="0,3,0,0"/></StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OverlayActionButton}" Click="OnEdit" Visibility="{Binding ImageOnlyVisibility}" AutomationProperties.Name="Edit screenshot">
                                                    <StackPanel><TextBlock Text="&#xE70F;" FontFamily="{StaticResource IconFont}" FontSize="16" HorizontalAlignment="Center"/><TextBlock Text="Edit" FontSize="10" Margin="0,3,0,0"/></StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OverlayActionButton}" Click="OnPin" Visibility="{Binding PinVisibility}" AutomationProperties.Name="Pin screenshot">
                                                    <StackPanel><TextBlock Text="&#xE840;" FontFamily="{StaticResource IconFont}" FontSize="16" HorizontalAlignment="Center"/><TextBlock Text="Pin" FontSize="10" Margin="0,3,0,0"/></StackPanel>
                                                </Button>
                                                <Button Style="{StaticResource OverlayActionButton}" Click="OnDelete" AutomationProperties.Name="Delete history item">
                                                    <StackPanel><TextBlock Text="&#xE74D;" FontFamily="{StaticResource IconFont}" FontSize="16" HorizontalAlignment="Center"/><TextBlock Text="Delete" FontSize="10" Margin="0,3,0,0"/></StackPanel>
                                                </Button>
                                            </StackPanel>
                                        </Border>

                                        <!-- Photo-picker pattern: selection mode shows an empty ring on every tile;
                                             selecting fills it accent with the check glyph. -->
                                        <Border x:Name="SelectionCheck" Width="24" Height="24" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8"
                                                Background="#66000000" BorderBrush="White" BorderThickness="1.5" CornerRadius="5" Visibility="Collapsed">
                                            <TextBlock x:Name="SelectionCheckGlyph" Text="&#xE73E;" FontFamily="{StaticResource IconFont}" Foreground="White" FontSize="13" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                        </Border>
                                    </Grid>
                                </Border>
                                <StackPanel Grid.Row="1" Margin="7,7,7,10" ToolTip="{Binding FileName}">
                                    <TextBlock Text="{Binding RelativeTime}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="12" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding MetadataText}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,3,0,0"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                        <DataTemplate.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True"/>
                                    <Condition Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="OverlayActions" Property="Visibility" Value="Visible"/>
                                <MultiDataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="OverlayActions" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.12">
                                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.EnterActions>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True"/>
                                    <Condition Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" Value="False"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="OverlayActions" Property="Visibility" Value="Visible"/>
                                <MultiDataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="OverlayActions" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.12">
                                                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
                                            </DoubleAnimation>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </MultiDataTrigger.EnterActions>
                            </MultiDataTrigger>
                            <DataTrigger Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" Value="True">
                                <Setter TargetName="SelectionCheck" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding Tag, RelativeSource={RelativeSource AncestorType={x:Type ListBox}}}" Value="True"/>
                                    <Condition Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter TargetName="SelectionCheck" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="SelectionCheck" Property="BorderThickness" Value="1"/>
                                <Setter TargetName="SelectionCheckGlyph" Property="Visibility" Value="Visible"/>
                            </MultiDataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <StackPanel x:Name="EmptyState" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center" IsHitTestVisible="False">
                <TextBlock Text="&#xE91B;" FontFamily="{StaticResource IconFont}" FontSize="48" Foreground="{StaticResource TextDisabledBrush}" HorizontalAlignment="Center"/>
                <TextBlock Text="No captures yet" Foreground="{StaticResource TextSecondaryBrush}" FontSize="15" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                <TextBlock Text="Screenshots and recordings you take will show up here." Foreground="{StaticResource TextDisabledBrush}" FontSize="12" Margin="0,6,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
