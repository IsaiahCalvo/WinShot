<Window x:Class="WinShot.History.HistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WinShot — History" Width="900" Height="600" MinWidth="480" MinHeight="360"
        Background="{StaticResource WindowBgBrush}" ShowInTaskbar="True" WindowStartupLocation="Manual"
        AutomationProperties.Name="WinShot history"
        AutomationProperties.HelpText="Browse, preview, open, copy, edit, pin, reveal, or delete local captures.">
    <Window.Resources>
        <!-- Small tile action button (Copy / Edit / Pin / Del), revealed on hover. -->
        <Style x:Key="TileButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Margin" Value="0,0,4,0"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                CornerRadius="{StaticResource RadiusButton}" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource ElevatedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Centered blue "Restore" pill overlaid on the selected card. -->
        <Style x:Key="RestorePill" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="16" Padding="16,8">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="12"
                                                  ShadowDepth="0" Opacity="0.45"/>
                            </Border.Effect>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                <!-- Refresh/restore glyph -->
                                <TextBlock Text="&#xE72C;" FontFamily="{StaticResource IconFont}"
                                           FontSize="13" Foreground="White"
                                           VerticalAlignment="Center" Margin="0,0,7,0"/>
                                <TextBlock Text="Restore" Foreground="White" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{StaticResource AccentHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="White"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- CleanShot-style filter pill: transparent/subtle when inactive,
             solid accent fill when the segment is selected. -->
        <Style x:Key="FilterChip" TargetType="RadioButton">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="3,0"/>
            <Setter Property="Focusable" Value="True"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="14" Padding="14,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SurfaceBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="{StaticResource ToolbarBgBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1" Padding="12,10">
            <!-- Centered segmented filter pills (CleanShot X style). -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <RadioButton x:Name="ChipAll" Content="All" IsChecked="True"
                             Style="{StaticResource FilterChip}" Checked="OnFilterChanged"
                             AutomationProperties.Name="Show all history items"
                             AutomationProperties.HelpText="Shows screenshots, videos, and GIFs."/>
                <RadioButton x:Name="ChipImages" Content="Screenshots"
                             Style="{StaticResource FilterChip}" Checked="OnFilterChanged"
                             AutomationProperties.Name="Show screenshots"
                             AutomationProperties.HelpText="Shows still-image captures."/>
                <RadioButton x:Name="ChipVideo" Content="Videos"
                             Style="{StaticResource FilterChip}" Checked="OnFilterChanged"
                             AutomationProperties.Name="Show videos"
                             AutomationProperties.HelpText="Shows video recordings."/>
                <RadioButton x:Name="ChipGif" Content="GIFs"
                             Style="{StaticResource FilterChip}" Checked="OnFilterChanged"
                             AutomationProperties.Name="Show GIFs"
                             AutomationProperties.HelpText="Shows animated GIF recordings."/>
            </StackPanel>
        </Border>
        <Border DockPanel.Dock="Bottom" Background="{StaticResource ToolbarBgBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0" Padding="12,8">
            <DockPanel>
                <Button DockPanel.Dock="Right" Content="Refresh" Margin="0"
                        Style="{StaticResource TileButton}" Click="OnRefresh"
                        AutomationProperties.Name="Refresh history"
                        AutomationProperties.HelpText="Reloads local history items from disk."/>
                <Button x:Name="LoadMoreButton" DockPanel.Dock="Right" Content="Load more"
                        Margin="0,0,8,0" Style="{StaticResource TileButton}"
                        Click="OnLoadMore" Visibility="Collapsed"
                        AutomationProperties.Name="Load more history items"
                        AutomationProperties.HelpText="Shows the next 200 local history items without loading the entire library at once."/>
                <TextBlock x:Name="CountText" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="12" VerticalAlignment="Center"
                           AutomationProperties.LiveSetting="Polite"/>
            </DockPanel>
        </Border>

        <Grid>
            <!-- Responsive thumbnail grid. WPF grouping does not reliably virtualize a
                 wrapping panel, so the backing model incrementally exposes 200 cards.
                 The full lightweight file model remains available for filters, grouping,
                 refresh, and selection restoration without creating 5,000 visual cards. -->
            <ListBox x:Name="ItemsList" Background="Transparent" BorderThickness="0" Padding="12"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     ScrollViewer.CanContentScroll="False"
                     KeyboardNavigation.TabNavigation="Continue"
                     AutomationProperties.Name="Capture history"
                     AutomationProperties.HelpText="Use arrow keys to select, Enter to open, Space to preview, and Tab to reach item actions.">
                <ListBox.ItemContainerStyle>
                    <!-- Real selection: containers are focusable so arrow keys move a
                         SelectedItem across the WrapPanel. The selection visual is a thin
                         accent ring around the card (the card keeps its own hover border). -->
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Focusable" Value="True"/>
                        <Setter Property="Margin" Value="0"/>
                        <Setter Property="Padding" Value="0"/>
                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                        <Setter Property="AutomationProperties.Name" Value="{Binding AccessibleName}"/>
                        <Setter Property="AutomationProperties.HelpText" Value="{Binding AccessibleHelpText}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <!-- The ring carries the inter-tile spacing (margin) and
                                     hugs the card edge; selection lights its accent border. -->
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border x:Name="SelectionRing" Margin="6"
                                            BorderBrush="Transparent" BorderThickness="2"
                                            CornerRadius="{StaticResource RadiusCard}"
                                            Background="Transparent">
                                        <Grid>
                                            <ContentPresenter/>
                                            <!-- Centered blue Restore pill, shown only on the
                                                 selected card. Wired to the existing open action. -->
                                            <Button x:Name="RestoreOverlay"
                                                    Style="{StaticResource RestorePill}"
                                                    HorizontalAlignment="Center" VerticalAlignment="Center"
                                                    Visibility="Collapsed"
                                                    AutomationProperties.Name="Open history item"
                                                    AutomationProperties.HelpText="Opens this capture in its default app."
                                                    Click="OnOpen"/>
                                        </Grid>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter TargetName="SelectionRing" Property="BorderBrush"
                                                    Value="{StaticResource AccentBrush}"/>
                                            <Setter TargetName="RestoreOverlay" Property="Visibility"
                                                    Value="Visible"/>
                                        </Trigger>
                                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                                            <Setter TargetName="SelectionRing" Property="BorderBrush"
                                                    Value="{StaticResource AccentBrush}"/>
                                            <Setter TargetName="SelectionRing" Property="Background"
                                                    Value="{StaticResource SurfaceBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <!-- Groups stack vertically (StackPanel); tiles wrap within each group. -->
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <!-- Small uppercase secondary-text day header. -->
                                <TextBlock Text="{Binding Name}" Margin="9,12,0,4"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           FontSize="11" FontWeight="SemiBold"/>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>
                        <GroupStyle.Panel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </GroupStyle.Panel>
                    </GroupStyle>
                </ListBox.GroupStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <!-- Square-ish card: large thumbnail + thin caption.
                             Action row revealed on hover (named OverlayActions). -->
                        <Border x:Name="Card" Width="190" Margin="0"
                                Background="{StaticResource ToolbarBgBrush}"
                                BorderBrush="{StaticResource BorderBrush}" BorderThickness="1"
                                CornerRadius="{StaticResource RadiusCard}" ClipToBounds="True"
                                MouseLeftButtonDown="OnTileMouseDown"
                                MouseMove="OnTileMouseMove"
                                MouseEnter="OnTileMouseEnter">
                            <Border.ContextMenu>
                                <ContextMenu Style="{StaticResource DarkContextMenu}">
                                    <MenuItem Header="Copy" Click="OnCopy" Visibility="{Binding ImageOnlyVisibility}" Style="{StaticResource DarkMenuItem}" AutomationProperties.HelpText="Copies this screenshot to the clipboard."/>
                                    <MenuItem Header="Edit" Click="OnEdit" Visibility="{Binding ImageOnlyVisibility}" Style="{StaticResource DarkMenuItem}" AutomationProperties.HelpText="Opens this screenshot in the annotation editor."/>
                                    <MenuItem Header="Pin" Click="OnPin" Visibility="{Binding PinVisibility}" Style="{StaticResource DarkMenuItem}" AutomationProperties.HelpText="Pins this still image above other windows."/>
                                    <MenuItem Header="Open" Click="OnOpen" Style="{StaticResource DarkMenuItem}" AutomationProperties.HelpText="Opens this capture in its default app."/>
                                    <MenuItem Header="Reveal in Explorer" Click="OnReveal" Style="{StaticResource DarkMenuItem}" AutomationProperties.HelpText="Shows this local history file in File Explorer."/>
                                    <Separator/>
                                    <MenuItem Header="Delete" Click="OnDelete" Style="{StaticResource DarkMenuItem}" AutomationProperties.HelpText="Deletes only this local history copy."/>
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="130"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Thumbnail fills the card top. -->
                                <Border Grid.Row="0" Background="{StaticResource WindowBgBrush}" ClipToBounds="True">
                                    <Grid>
                                        <Image Source="{Binding Thumbnail}" Stretch="UniformToFill"
                                               Visibility="{Binding ImageOnlyVisibility}"/>
                                        <TextBlock Text="{Binding ExtensionLabel}" Visibility="{Binding MediaOnlyVisibility}"
                                                   Foreground="{StaticResource AccentBrush}" FontSize="28" FontWeight="Bold"
                                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                        <!-- Hover action row, anchored bottom of the thumbnail. -->
                                        <Border x:Name="OverlayActions" VerticalAlignment="Bottom"
                                                Background="#CC000000" Padding="6,5" Visibility="Collapsed">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                                <Button Content="Copy" Style="{StaticResource TileButton}" Click="OnCopy"
                                                        Visibility="{Binding ImageOnlyVisibility}"
                                                        AutomationProperties.Name="Copy screenshot"
                                                        AutomationProperties.HelpText="Copies this screenshot to the clipboard."/>
                                                <Button Content="Edit" Style="{StaticResource TileButton}" Click="OnEdit"
                                                        Visibility="{Binding ImageOnlyVisibility}"
                                                        AutomationProperties.Name="Edit screenshot"
                                                        AutomationProperties.HelpText="Opens this screenshot in the annotation editor."/>
                                                <Button Content="Pin" Style="{StaticResource TileButton}" Click="OnPin"
                                                        Visibility="{Binding PinVisibility}"
                                                        AutomationProperties.Name="Pin screenshot"
                                                        AutomationProperties.HelpText="Pins this still image above other windows."/>
                                                <Button Content="Delete" Style="{StaticResource TileButton}" Click="OnDelete"
                                                        AutomationProperties.Name="Delete history item"
                                                        AutomationProperties.HelpText="Deletes only this local history copy."/>
                                            </StackPanel>
                                        </Border>
                                    </Grid>
                                </Border>

                                <!-- Centered relative-time caption ("1 minute ago"),
                                     CleanShot X style. Filename/size kept as the tooltip. -->
                                <StackPanel Grid.Row="1" Margin="10,8,10,10">
                                    <TextBlock Text="{Binding RelativeTime}" Foreground="{StaticResource TextSecondaryBrush}"
                                               FontSize="12" HorizontalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"
                                               ToolTip="{Binding Caption}"/>
                                </StackPanel>
                            </Grid>
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                        </Border>
                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True">
                                <Setter TargetName="OverlayActions" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}}" Value="True">
                                <Setter TargetName="OverlayActions" Property="Visibility" Value="Visible"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!-- Empty state: centered hint when there are no items. -->
            <StackPanel x:Name="EmptyState" Visibility="Collapsed"
                        HorizontalAlignment="Center" VerticalAlignment="Center"
                        IsHitTestVisible="False">
                <TextBlock Text="&#xE91B;" FontFamily="{StaticResource IconFont}"
                           FontSize="48" Foreground="{StaticResource TextDisabledBrush}"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="No captures yet" Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="15" Margin="0,12,0,0" HorizontalAlignment="Center"/>
                <TextBlock Text="Screenshots and recordings you take will show up here."
                           Foreground="{StaticResource TextDisabledBrush}"
                           FontSize="12" Margin="0,6,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </DockPanel>
</Window>
