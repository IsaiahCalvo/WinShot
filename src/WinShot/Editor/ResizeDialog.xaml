<Window x:Class="WinShot.Editor.ResizeDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Resize image" Background="{StaticResource WindowBgBrush}"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False"
        UseLayoutRounding="True">
    <StackPanel Margin="18,16">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Width (px)" Style="{StaticResource FieldLabel}"
                       Margin="0,0,10,8"/>
            <TextBox x:Name="WidthBox" Grid.Row="0" Grid.Column="1" Style="{StaticResource DarkTextBox}"
                     Width="76" Margin="0,0,0,8" TextChanged="OnWidthChanged"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Height (px)" Style="{StaticResource FieldLabel}"
                       Margin="0,0,10,8"/>
            <TextBox x:Name="HeightBox" Grid.Row="1" Grid.Column="1" Style="{StaticResource DarkTextBox}"
                     Width="76" Margin="0,0,0,8" TextChanged="OnHeightChanged"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Scale (%)" Style="{StaticResource FieldLabel}"
                       Margin="0,0,10,0"/>
            <TextBox x:Name="PercentBox" Grid.Row="2" Grid.Column="1" Style="{StaticResource DarkTextBox}"
                     Width="76" TextChanged="OnPercentChanged"/>
            <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal" Margin="8,0,0,0"
                        VerticalAlignment="Center">
                <Button Style="{StaticResource PillButtonSecondary}" Content="50%" Tag="50" Click="OnQuickPercent"/>
                <Button Style="{StaticResource PillButtonSecondary}" Content="200%" Tag="200" Click="OnQuickPercent"/>
            </StackPanel>
        </Grid>

        <CheckBox x:Name="LockRatio" Content="Lock aspect ratio" IsChecked="True"
                  Style="{StaticResource DarkCheckBox}" FontSize="12" Margin="0,10,0,0"/>

        <TextBlock x:Name="InfoText" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11" Margin="0,8,0,0"/>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button Style="{StaticResource PillButtonPrimary}" Content="Resize" Click="OnOk" IsDefault="True"/>
            <Button Style="{StaticResource PillButtonSecondary}" Content="Cancel" IsCancel="True"/>
        </StackPanel>
    </StackPanel>
</Window>
